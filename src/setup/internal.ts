export type Endpoints = {
  api: string,
  lobby: string,
  user: string
}


export const setup = {
  endpoints: {
    api: "https://runfission.com",
    lobby: "https://auth.fission.codes",
    user: "fission.name"
  },

  ipfs: {}
}
